[package]
name = "disk-insight"
version = "1.0.0"
edition = "2021"

[lib]
name = "disk_insight"
path = "src/lib.rs"

[[bin]]
name = "disk-insight"
path = "src/main.rs"

[dependencies]
windows = { version = "0.61", features = [
    "Win32_Foundation",
    "Win32_Storage_FileSystem",
    "Win32_System_Ioctl",
    "Win32_System_IO",
    "Win32_Security",
    "Win32_System_Threading",
] }
anyhow     = "1"    # エラー処理
byteorder  = "1"    # バッファのバイト列読み取り
rayon      = "1"    # 並列パース
serde      = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
opt-level = 3
lto = true